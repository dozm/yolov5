
# Train command: python train.py --data captcha_data/captcha.yaml --epochs 200

# download command/URL (optional)
download: ~

# train and val data as 1) directory: path/images/, 2) file: path/images.txt, or 3) list: [path1/images/, path2/images/]
train: ../captcha/images/train/
val: ../captcha/images/train/

# number of classes
nc: 1

# class names
names: ['gap']
